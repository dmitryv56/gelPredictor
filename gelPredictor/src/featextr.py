#!/usr/bin/env python3

import logging

import pandas as pd
import numpy as np

# train, validate, test

# normalization (training + validation)

#vector (day data) classification . The goal is each day data(vector) shall obtain a desire label (class number)

# vector to scalogram (image)

# neuron net (convolution) training and validationg: input-images(scalogram), output - desired labels.

# test data. predict on base neuron net (forward propogation). Each day (vector) in test dataset should 



# vector to scalogram (image)

if __name__ == "__main__":
    pass